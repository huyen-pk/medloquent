# For full-featured build (Coqui TTS, Vosk ASR, augmentations), install:
#   pip install -r requirements-full.txt
TTS
audiomentations
jiwer
soundfile
librosa
numpy
scipy
# ASR
vosk
# Optional / heavier libs (install as needed):
# openai-whisper
# onnxruntime
