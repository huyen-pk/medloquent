# For Kokoro ONNX TTS, Vosk ASR, and augmentations, install:
#   pip install -r requirements-full.txt
huggingface-hub
onnxruntime
misaki[en]
phonemizer-fork
audiomentations
jiwer
soundfile
librosa
numpy
scipy
# ASR
vosk
# Optional / heavier libs (install as needed):
# openai-whisper
